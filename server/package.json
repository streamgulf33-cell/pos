{
  "name": "pharmadesk-server",
  "version": "1.0.0",
  "description": "Central server for PharmaDesk — every device/branch reads and writes here instead of storing data locally.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.13"
  }
}
